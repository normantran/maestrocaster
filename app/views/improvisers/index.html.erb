<h1>Listing improvisers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @improvisers.each do |improviser| %>
      <tr>
        <td><%= improviser.name %></td>
        <td><%= link_to 'Show', improviser %></td>
        <td><%= link_to 'Edit', edit_improviser_path(improviser) %></td>
        <td><%= link_to 'Destroy', improviser, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Improviser', new_improviser_path %>

<%= form_for(@improviser) do |f| %>
  <% if @improviser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@improviser.errors.count, "error") %> prohibited this improviser from being saved:</h2>

      <ul>
      <% @improviser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

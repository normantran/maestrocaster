<p id="notice"><%= notice %></p>

<p>
  <strong>Date:</strong>
  <%= @show.show_date %>
</p>

<p>
  <strong>Players:</strong>
  <% @show.plays.each do |play| %>
    player
  <% end %>
</p>

<p>
  <strong>Add Improviser</strong>
  <%= render "plays/form"%>
</p>

<%= link_to 'Edit', edit_show_path(@show) %> |
<%= link_to 'Back', shows_path %>
